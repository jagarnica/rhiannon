use crate::audioproccesing as ap;
use crate::utils::cli::StartOpts;

pub fn handle_start(verbose: bool, opts: StartOpts) {
    let port_number = opts;
    // eprint("Starting the bot..{:?},",port_number );
}
